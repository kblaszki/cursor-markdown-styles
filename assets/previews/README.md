# Theme Preview Assets

Store GitHub-friendly preview screenshots in this directory.

Recommended convention:

- one screenshot per theme
- always render the same sample document: `examples/theme-preview.md`
- use predictable filenames such as `cursor-inline-github.png` or `cursor-inline-lumina.png`

This keeps theme comparisons fair and easy to browse from the repository homepage.
